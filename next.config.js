/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['cdn.cookielaw.org','raw.githubusercontent.com','assets.nflxext.com'
  ]}
}

module.exports = nextConfig
